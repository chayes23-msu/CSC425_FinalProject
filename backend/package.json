{
  "name": "backend",
  "version": "1.0.0",
  "description": "The database (SQLite) and server (Express) for our cattle data management app.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Isaac Reynolds and Matthew Hayes",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^11.5.0"
  }
}
